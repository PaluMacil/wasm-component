package article

// ArticleFactory builds a TemplateRef of ArticleComponent
type ArticleFactory struct {
}

// ArticleState hold state about the BlogComponent
type ArticleState struct {
	Text string
}

// ArticleComponent holds the
type ArticleComponent struct {
	//State wc.State[ArticleState]
}
